//
// CONSOLE I/O
//
asm cout(c)#0
    LDA ESTKL,X
    JMP $FFEE
end
def prstr(str)#0
    byte i, c
    i = 1
    while i <= ^str
        c=(str)[i]
        cout(c)
        i = i + 1
    loop
end
prstr("Hello world!\r\n")
done
