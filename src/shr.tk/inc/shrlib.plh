import shrlib
    predef shrPeek(addr)#1
    predef shrPoke(addr, data)#0
    predef shrSet(addr, data, len)#0
    predef shrPixel(x, y)#0
    predef shrColor(clr)#0
    predef shrSCB(row, scb)#0
    predef shrPalette(pal, rgbptr)#0
    predef shrMode(mode)#0
end