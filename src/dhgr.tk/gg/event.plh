import event
    struc t_event
        var evDelta
        var evAction
        var evParam
        var evNext
    end
    predef eventInit#0
    predef eventFree(event)#0
    predef eventNew#1
    predef doEvents#0
    predef addEvent(time, event)#0
    predef addAction(time, action, param)#0
    predef dumpEvents#0
end